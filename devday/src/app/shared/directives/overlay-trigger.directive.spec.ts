import { OverlayTriggerDirective } from './overlay-trigger.directive';

describe('OverlayTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
